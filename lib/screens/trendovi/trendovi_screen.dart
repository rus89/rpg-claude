// ABOUTME: Trendovi screen — shows active holdings over time as a line chart.
// ABOUTME: Placeholder until Task 11 is implemented.

import 'package:flutter/material.dart';

class TrendoviScreen extends StatelessWidget {
  const TrendoviScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Trendovi')));
  }
}
